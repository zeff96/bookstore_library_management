<div class="w-full h-screen ms-3">
  <h1 class="font-bold text-4xl text-center">Non Fictions</h1>
  <div>
    <ul class="h-screen">
      <% @books.each do |book|%>
        <li>
          <h2><%= book.title %></h2>
          <p><%= book.author %></p>
        </li>
      <% end %>
    </ul>
  </div>
</div>
